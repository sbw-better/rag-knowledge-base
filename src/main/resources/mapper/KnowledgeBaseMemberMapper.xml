<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.rag.mapper.KnowledgeBaseMemberMapper">
    <select id="countByKnowledgeBaseIdAndUserId" resultType="int">
        select count(1) from knowledge_base_members
        where knowledge_base_id = #{knowledgeBaseId} and user_id = #{userId}
    </select>

    <select id="selectByKnowledgeBaseId" resultType="com.example.rag.domain.KnowledgeBaseMember">
        select * from knowledge_base_members
        where knowledge_base_id = #{knowledgeBaseId}
        order by created_at desc
    </select>

    <select id="selectByKnowledgeBaseIdAndUserId" resultType="com.example.rag.domain.KnowledgeBaseMember">
        select * from knowledge_base_members
        where knowledge_base_id = #{knowledgeBaseId} and user_id = #{userId}
        limit 1
    </select>

    <delete id="deleteByKnowledgeBaseIdAndUserId">
        delete from knowledge_base_members
        where knowledge_base_id = #{knowledgeBaseId} and user_id = #{userId}
    </delete>
</mapper>
