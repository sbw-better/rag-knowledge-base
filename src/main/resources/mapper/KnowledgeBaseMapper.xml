<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.rag.mapper.KnowledgeBaseMapper">
    <select id="selectVisibleByTenantId" resultType="com.example.rag.domain.KnowledgeBase">
        select * from knowledge_bases
        where tenant_id = #{tenantId} and deleted = 0
        order by created_at desc
    </select>

    <select id="selectByIdAndTenantIdNotDeleted" resultType="com.example.rag.domain.KnowledgeBase">
        select * from knowledge_bases
        where id = #{id} and tenant_id = #{tenantId} and deleted = 0
        limit 1
    </select>

    <select id="countActiveByTenantId" resultType="long">
        select count(1) from knowledge_bases
        where tenant_id = #{tenantId} and deleted = 0
    </select>
</mapper>
