<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.rag.mapper.MessageCitationMapper">
    <select id="selectByMessageId" resultType="com.example.rag.domain.MessageCitation">
        select * from message_citations
        where message_id = #{messageId}
        order by created_at asc
    </select>

    <delete id="deleteByDocumentId">
        delete from message_citations
        where document_id = #{documentId}
    </delete>

    <delete id="deleteByConversationId">
        delete from message_citations
        where message_id in (
            select id from messages where conversation_id = #{conversationId}
        )
    </delete>
</mapper>
