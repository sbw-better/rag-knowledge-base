<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.rag.mapper.AnswerFeedbackMapper">
    <select id="selectByAssistantMessageIdAndUserId" resultType="com.example.rag.domain.AnswerFeedback">
        SELECT *
        FROM answer_feedback
        WHERE assistant_message_id = #{assistantMessageId}
          AND user_id = #{userId}
        LIMIT 1
    </select>

    <select id="selectByBusiness" resultType="com.example.rag.domain.AnswerFeedback">
        SELECT *
        FROM answer_feedback
        WHERE tenant_id = #{tenantId}
          AND knowledge_base_id = #{knowledgeBaseId}
          AND business_module = #{businessModule}
          AND business_entity_id = #{businessEntityId}
        ORDER BY updated_at DESC
        LIMIT #{limit}
    </select>
</mapper>
